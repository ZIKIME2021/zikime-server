import React, { Fragment } from 'react';
import Button from '../common/Button';

const LoginPage = () => {
    return (
        <Fragment>
        <div>로그인</div>
        </Fragment>
    );
};

export default LoginPage;